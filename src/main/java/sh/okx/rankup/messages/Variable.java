package sh.okx.rankup.messages;

public enum Variable {
  PLAYER,
  OLD_RANK,
  OLD_RANK_NAME,
  RANK,
  RANK_NAME,
  FROM,
  TO,
  MONEY,
  MONEY_NEEDED,
  AMOUNT,
  AMOUNT_NEEDED,
  PERCENT_DONE,
  PERCENT_LEFT,
  SECONDS,
  SECONDS_LEFT
}
